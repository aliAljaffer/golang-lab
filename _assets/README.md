# _assets

Diagrams, screenshots, and images referenced from notes.

The underscore prefix tells Go's tooling to ignore this directory (it won't be scanned for `.go` files). Obsidian and GitHub will still render images linked from this folder normally.

Naming convention: `<section>-<short-description>.<ext>` — e.g. `05-concurrency-channel-flow.png`.
