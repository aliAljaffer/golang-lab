// Package main is the entry point for executables.
// Every Go file declares a package; main is special — it produces a runnable program.
package main

import "fmt"

// main is the entry point.
// Analog: Python `if __name__ == "__main__":`, Java `public static void main`.
func main() {
	fmt.Println("hello, devops")
}
