# Exercises — 08-kubernetes

Each subfolder is an exercise with failing tests. Tests against real clusters are tagged `integration`; unit tests use the `fake` clientset.

See parent [`PLAN.md`](../PLAN.md).
